{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.9"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "buildCommand": "apt-get update && apt-get install -y python3.9 python3.9-distutils && curl -sS https://bootstrap.pypa.io/get-pip.py | python3.9 && python3.9 -m pip install --upgrade pip setuptools wheel && python3.9 -m pip install -r requirements.txt"
}
