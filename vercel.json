{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "build": {
    "env": {
      "PYTHON_VERSION": "3.10"
    }
  },
  "buildCommand": "apt-get update && apt-get install -y python3-distutils python3.10-venv && python3.10 -m ensurepip && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
}
