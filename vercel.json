{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.9"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "buildCommand": "apt-get update && apt-get install -y python3-distutils && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
}
